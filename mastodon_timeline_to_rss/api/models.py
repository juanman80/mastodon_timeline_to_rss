from django.db import models
"""
should adapt the ruby models in https://github.com/mastodon/mastodon/tree/main/app/models
also add the models to generate an rss entry
"""


# Create your models here.
# class TimelineStatus(models.Model):
#     id = models.BigIntegerField(primary_key=True)
#     status = models.TextField()
#     received = models.DateTimeField(auto_now=True)
